import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  banner: {
    borderLeftWidth: 4,
    padding: 12,
    borderRadius: 8,
    marginBottom: 14,
  },
  statusText: {
    fontSize: 12,
    fontWeight: '600',
    textAlign: 'center',
  },
});