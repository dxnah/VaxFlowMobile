# Copy to .env and fill in your values
# For local dev: your machine's LAN IP (run `ipconfig` / `ifconfig` to find it)
API_URL=http://192.168.1.245:8000/api

# For EAS Build / production: your deployed backend URL
# BASE_URL=https://your-backend.railway.app/api
